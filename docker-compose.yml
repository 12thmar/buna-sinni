services:
  frontend:
    build:
      context: ./frontend
      target: runtime       # 👈 nginx stage
    container_name: buna-sinni
    ports:
      - "80:80"
    networks:
      - app-net

networks:
  app-net:
    driver: bridge
